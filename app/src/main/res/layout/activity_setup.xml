<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:padding="@dimen/default_text_view_padding"
        android:text="@string/assistant_setup"
        android:textAppearance="@style/TextAppearance.WaveAssistant.AssistantText" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/prefsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@android:color/transparent"
        tools:listitem="@layout/preference_item" />

    <Button
        android:id="@+id/completeSetup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginVertical="15dp"
        android:background="@drawable/button_background"
        android:padding="@dimen/default_view_padding"
        android:text="Complete Setup"
        android:textAllCaps="false"
        android:textAppearance="@style/TextAppearance.WaveAssistant.Button" />


</LinearLayout>